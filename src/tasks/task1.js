alert('Доброе утро');

const sayHello = (name) => {
    if (typeof name === "string") {

        return `Hello, ${name}`;
    }
}
const name = "Oleg"

console.log(sayHello(name));

sayHellow = function (name) {
    if (typeof name === "string") {

        return `Hello, ${name}`;
    }
}
alert(sayHellow('Victor'));
alert('Доброе утро');
