const numberArray = [1, 2, 3, 4];
if (!Array.isArray(numberArray)) return;
const userNumber = (array) => {
    {
        return array.map((numberArray)) => {
    return `User ${number.toString()}`;
}



console.log(userNumber());